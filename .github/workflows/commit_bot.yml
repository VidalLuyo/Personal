name: Bot de Commits

on:
  schedule:
<<<<<<< HEAD
    - cron: "30 19 * * *" # 2:30 pm en Perú (7:30 pm UTC)
    - cron: "35 19 * * *" # 2:35 pm en Perú (7:35 pm UTC)
    - cron: "40 19 * * *" # 2:40 pm en Perú (7:40 pm UTC)
  workflow_dispatch: # Esto permite ejecutar el workflow manualmente desde GitHub
=======
    - cron: "0 18 * * *"  # 1:00 pm en Perú (6:00 pm UTC)
    - cron: "5 18 * * *"  # 1:05 pm en Perú (6:05 pm UTC)
    - cron: "10 18 * * *" # 1:10 pm en Perú (6:10 pm UTC)
    - cron: "15 18 * * *" # 1:15 pm en Perú (6:15 pm UTC)
    - cron: "20 18 * * *" # 1:20 pm en Perú (6:20 pm UTC)
  workflow_dispatch: # Permite ejecutar el workflow manualmente
>>>>>>> 6f4ba7a0bbc7624fc5577a8a1ab2762c294b9299

jobs:
  commit:
    permissions: 
      contents: write # Permiso para hacer push al repositorio
    runs-on: ubuntu-latest
    timeout-minutes: 5
    
    steps:
      - name: Checkout del repositorio
        uses: actions/checkout@v2

      - name: Configurar información de usuario de Git
        run: |
          git config --global user.email "jesus.luyoc@vallegrande.edu.pe"
          git config --global user.name "JesúsVidalLuyo"

      - name: Crear y hacer commit automático con contador
        run: |
          # Obtener el número de la última línea o iniciar en 1 si el archivo no existe
          if [ -f "Mis commits Personales.txt" ]; then
            count=$(wc -l < "Mis commits Personales.txt")
            count=$((count + 1))
          else
            count=1
          fi
          echo "Mi Commit diario $count" >> "Mis commits Personales.txt"
          git add "Mis commits Personales.txt"
          git commit -m "Mi commit diario $count"

      - name: Push de los cambios
        env:
          GITHUB_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          git push https://$GITHUB_TOKEN@github.com/VidalLuyo/Personal.git
